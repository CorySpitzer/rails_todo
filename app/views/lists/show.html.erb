<h1><%= @list.title %></h1>

<%= link_to 'Edit list', edit_list_path(@list)%>
<!-- Works without arg. sometimes? -->
<p><%= link_to "Delete List", list_path(@list),
                        :data => {:confirm => "Are you sure?",
                        :method => "delete"},
                        :class => "btn btn-danger" %></p>
